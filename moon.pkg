import {
  "moonbitlang/async/types" @async/types,
  "moonbitlang/async/fs" @async/fs,
  "moonbitlang/async/stdio" @async/stdio,
}

import {
  "moonbitlang/async",
} for "test"

supported_targets = "native"

options(
  "native-stub": [ "isatty.c" ],
)
